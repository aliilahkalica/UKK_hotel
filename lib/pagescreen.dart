import 'package:flutter/material.dart';

class PageScreen extends StatelessWidget {
  const PageScreen({super.key, required String title});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
